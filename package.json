{
  "name": "gmail-mcp-server",
  "version": "1.0.0",
  "description": "Custom Gmail MCP server for ClickUp Brain",
  "main": "src/index.js",
  "scripts": {
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  }
}
